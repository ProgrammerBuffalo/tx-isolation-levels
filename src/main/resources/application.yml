spring:
  datasource:
   url: jdbc:mysql://localhost:3306/transaction-levels-db
   username: admin
   password: admin
   driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
   hibernate.ddl-auto: create-drop
   show-sql: true
   properties:
    hibernate:
     dialect: org.hibernate.dialect.MySQL8Dialect